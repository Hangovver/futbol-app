import React from 'react'; import { View, Text } from 'react-native'; import { theme } from '../theme'; export default function Badge({ label }: {label:string}){ const t=theme.dark; return (<View style={{ backgroundColor:t.chip, paddingHorizontal:10, paddingVertical:6, borderRadius:16, marginRight:6, marginBottom:6 }}><Text style={{ color:t.chipText, fontWeight:'600' }}>{label}</Text></View>); }