import React from 'react'; import { View, Text } from 'react-native'; import Header from '../components/Header'; import { theme } from '../theme'; export default function Settings(){ const t=theme.dark; return (<View style={{ flex:1, backgroundColor:t.bg, padding:16 }}><Header title='Ayarlar' /><Text style={{ color:t.sub }}>API_BASE’i app/app.json içinde ayarlayın.</Text></View>); }